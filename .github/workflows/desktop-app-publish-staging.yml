name: Desktop App Publish Staging

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    name: Build Staging
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Mock build staging
        run: |
          echo "🔨 Building staging..."
          echo "Version: $(jq -r '.version' src-tauri/tauri.conf.staging.json)"
          sleep 5
          echo "✅ Staging build complete (mock)"

