name: Desktop App Publish Staging

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    name: Build Staging
    runs-on: ubuntu-latest
    environment: staging  # ← Approval gate!
    steps:
      - uses: actions/checkout@v4
      
      - name: Build staging
        run: |
          echo "🔨 Building staging..."
          echo "Version: $(jq -r '.version' src-tauri/tauri.conf.staging.json)"
          sleep 3
          echo "✅ Staging build complete"
