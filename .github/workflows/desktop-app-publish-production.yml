name: Desktop App Publish Production

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    name: Build Production
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Mock build production
        run: |
          echo "🚀 Building production..."
          echo "Version: $(jq -r '.version' src-tauri/tauri.conf.production.json)"
          sleep 5
          echo "✅ Production build complete (mock)"

